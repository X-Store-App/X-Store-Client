import DynamicEmitter from './DynamicEmitter'
import StaticEmitter from './StaticEmitter'

// You can import this file with the webpack alias 'API' in both renderer and main process

export default { DynamicEmitter, StaticEmitter }
